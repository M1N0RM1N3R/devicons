---
title: "Contributing"
description: "Add a missing icon, improve an existing glyph, or help us refine the grid."
order: 0
category: "Community"
categoryOrder: 4
---

Devicons is community-built. Every icon, metadata entry, and tooling improvement lands through pull requests on GitHub.

## Before you open a PR

1. **Search first.** Check the [icon catalog](/icons) and open issues to avoid duplicates.
2. **Follow the grid.** Icons are drawn on a 24×24 grid with a 1.5px stroke by default. Larger marks use 2px.
3. **Check brand guidelines.** If the icon represents a company or product, link the guideline you followed.

## Adding an icon

Drop a single, optimized SVG into `packages/icons/src/<slug>.svg`. Then add metadata:

```yaml
---
name: "Your Icon"
description: "One sentence about what this represents."
icons: [your-icon]
tags: ["category", "related-keywords"]
website: "https://example.com"
---
```

Run the build to regenerate packages:

```bash
pnpm build
```

## Improving an existing icon

- Preserve the original icon IDs (they're public API).
- Match the visual weight of nearby icons — the new version should sit cleanly next to its siblings.
- Test both the colored and monochrome variants before opening the PR.

## Tooling

- **Formatting** — `pnpm format` runs Prettier + SVGO.
- **Preview** — `pnpm dev` spins up this website locally with your changes live.
- **Screenshot diff** — `pnpm test:visual` catches unintended regressions across the grid.

## Code of conduct

Be kind. Assume good intent. Celebrate contributors whose first PR is just fixing a typo — those matter.
